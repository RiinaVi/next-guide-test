export type Show = {
    id: number,
    name: string,
    image: Image,
    summary: string
}

type Image = {
    medium: string
}

 export type InputFile = {
    current: any
}

type Current = {
    value : any
}
